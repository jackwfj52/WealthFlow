spring:
  application:
    name: WealthFlow

  datasource:
    url: jdbc:sqlite:wealthflow.db
    driver-class-name: org.sqlite.JDBC

  sql:
    init:
      mode: never

mybatis:
  mapper-locations: classpath:mapper/*.xml